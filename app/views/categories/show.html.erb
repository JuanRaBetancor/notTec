<div class="container">
	<p id="notice"><%= notice %></p>
	<h1 style="color:<%= @category.color %>;"> <%= @category.name %></h1>
	<div class="row top-space text-center red">
    	<%= link_to 'Edit', edit_category_path(@category) %> 
		<%= link_to 'Back', categories_path %>
    </div>
	<h2>Noticias</h2>


	<div class="row">
		<%= render @category.articles %>
	</div>
	
</div>